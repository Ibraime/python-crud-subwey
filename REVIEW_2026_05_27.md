# Revisión del proyecto — Ibraime (Subwey CRUD)

**Fuente de verdad:** `proyecto/05-flask-04/Subwey/` (fases 01-03), `proyecto/04-sqlite/` (fase 04)
**Fases revisadas:** 02, 04
**Fecha de revisión:** 2026-05-27
**Revisión anterior:** `REVIEW_2026_04_15.md`

## REVISIÓN FASE 02 - 2026-05-27 — Nota: 10/10

### Resuelto desde la revisión anterior

- **Docstrings en `application/servicios_bocadillo.py`** — Resuelto. Añadido el docstring de módulo, el de la clase `ServicioBocadillo` y los de los 7 métodos públicos (`__init__`, `crear_bocadillo`, `modificar_bocadillo`, `eliminar_bocadillo`, `listar_bocadillos`, `obtener_bocadillo`, `es_promocional`). Todos incluyen descripción, parámetros y valor de retorno.
- **Docstrings en `application/servicios_ingrediente.py`** — Resuelto. Añadido el docstring de módulo y todos los de clase y métodos (`__init__`, `registrar_ingrediente`, `reponer_ingrediente`, `consumir_ingrediente`, `eliminar_ingrediente`, `listar_ingredientes`, `buscar_por_nombre`).
- **Docstrings en `application/servicios_usuario.py`** — Resuelto. La clase y todos sus métodos tienen docstrings completos.
- **Docstrings en `domain/usuario.py`** — Resuelto. Tiene docstring de módulo, de la clase `Usuario` y de los métodos `__init__` y `__repr__`.
- **Comentarios redundantes eliminados** — Resuelto. Los comentarios obvios del tipo `# Añade un bocadillo a la lista` han desaparecido de `servicios_bocadillo.py`, `servicios_ingrediente.py` y `menu_ingredientes.py`. Se conserva el comentario importante "Para mantener consistencia de datos al cambiar de menú" en [menu_ingredientes.py:46](proyecto/05-flask-04/Subwey/presentation/menu_ingredientes.py#L46), que sí aportaba valor.

Has cerrado todos los puntos de la revisión anterior. Buen trabajo.

## REVISIÓN FASE 04 - 2026-05-27 — Nota: 9/10

### Cumple

- Esquema SQL bien diseñado en `crear_bd.py`: tablas `ingredientes`, `usuarios`, `bocadillos` y la N:M `bocadillo_ingredientes`, con `PRIMARY KEY`, `NOT NULL`, `CHECK` para precio/stock, `FOREIGN KEY` entre bocadillos y usuarios. Columnas en `snake_case`.
- Script idempotente: usa `CREATE TABLE IF NOT EXISTS` + `INSERT OR REPLACE` para los datos iniciales. Se puede ejecutar varias veces sin error.
- Activa `PRAGMA foreign_keys = ON` tanto en `crear_bd.py` como en cada repositorio antes de ejecutar consultas.
- Consultas parametrizadas con `?` en los tres repositorios (`repositorio_bocadillo`, `repositorio_ingrediente`, `repositorio_usuario`).
- Excepciones de dominio para persistencia: `infrastructure/errores.py` con `BocadilloDuplicadoError`, capturando `sqlite3.IntegrityError`.
- La presentación no importa `sqlite3` y consume directamente los repositorios SQLite. Sin presencia del repositorio en memoria en el flujo principal.
- `CHANGELOG.md` con entradas `[0.4.0]` y `[0.4.1]` que describen lo añadido y cambiado.
- `README.md` y `docs/EJECUCION.md` actualizados con los pasos del script `crear_bd.py`.

### Pendiente

- **Falta `docs/DISEÑO_BD.md`.** La checklist pide documentar el diseño del esquema en este fichero. *Cómo arreglarlo:* crear el documento con el listado de tablas, sus columnas, tipos, restricciones y relaciones. La parte difícil ya está hecha en `crear_bd.py`.
- **Comando de ejecución del `README.md` incorrecto.** Aparece `python -m crear_bd.py`. Con `-m` se pasa un módulo (sin extensión); con la extensión, el comando es `python crear_bd.py`. *Cómo arreglarlo:* sustituir por `python crear_bd.py` o `python -m crear_bd` (el primero es más claro).

### Verificación funcional

La aplicación arranca con `python -m Subwey.presentation.menu_ingredientes`, los datos iniciales se cargan desde la BD y los cambios persisten al reabrir. Los flujos principales (registrar ingrediente, crear bocadillo, listar, eliminar) responden con los mensajes correctos y los duplicados disparan el error esperado.
