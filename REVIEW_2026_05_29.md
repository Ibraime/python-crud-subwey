# Revisión del proyecto — Ibraime (Subwey CRUD)

**Fuente de verdad:** `proyecto/05-flask-05/Subwey/` (también para F04, por la regla de la carpeta `NN-*` más alta).
**Fecha de revisión:** 2026-05-29.
**Revisión anterior:** `REVIEW_2026_05_27.md` (F02 10 / F04 9 / F05 10).
**Notas tras esta revisión:** F01 10 / F02 10 / F03 10 / F04 10 / F05 10.

Se reevalúan F04 y F05; F01, F02 y F03 no se reevalúan porque ya estaban en 10/10 y no han tenido cambios desde la revisión anterior.

## REVISIÓN FASE 04 - 2026-05-29 — Nota: 10/10

### Cumple (resuelto desde la revisión anterior)

- **`docs/DISEÑO_BD.md` creado.** El documento describe las cuatro tablas (`ingredientes`, `usuarios`, `bocadillos`, `bocadillo_ingredientes`), tipos de claves primarias TEXT y la clave compuesta + foráneas de la tabla intermedia.
- **Comando de ejecución del README corregido.** Ya no aparece `python -m crear_bd.py`; ahora indica `python crear_bd.py` correctamente.

### Cumple (re-validado)

- Esquema SQL coherente con claves primarias TEXT en las tres entidades, restricciones `CHECK` en precio y stock, `FOREIGN KEY` en `bocadillos.autor_nombre` y en la tabla intermedia.
- Script `crear_bd.py` idempotente (`CREATE TABLE IF NOT EXISTS` + `INSERT OR REPLACE INTO`) con datos iniciales razonables.
- `PRAGMA foreign_keys = ON` activado en `_conectar()` de cada repositorio.
- Jerarquía completa de excepciones de dominio en `infrastructure/errores.py`: `IngredienteDuplicadoError`, `BocadilloDuplicadoError`, `UsuarioDuplicadoError`, `IngredienteEnUsoError`, `UsuarioEnUsoError` y `BocadilloSinIngredientesError`. Los repositorios capturan `sqlite3.IntegrityError` y traducen al error de dominio correcto.
- Cero asignaciones a atributos protegidos del dominio desde fuera; los repositorios reconstruyen entidades llamando a constructores públicos.
- Discriminación de subclase `BocadilloPromocion` mediante la **columna `es_promocion`** en la tabla, no por `isinstance` — el repositorio simplemente lee `es_promo` y elige la clase a instanciar.

### Verificación funcional

Se ejecutó `python crear_bd.py` seguido de `python -m Subwey.presentation.app`. La base de datos se inicializa con 3 ingredientes, 3 usuarios y 2 bocadillos con sus relaciones. El menú CLI y la web acceden correctamente a los mismos datos.

## REVISIÓN FASE 05 - 2026-05-29 — Nota: 10/10

**Subfase entregada:** `05-flask-05` (la quinta y última, completa; sube desde `05-flask-04` que era lo evaluado en la revisión anterior).

### Cumple (nuevo desde la revisión anterior)

- **Subfase 05 nueva entregada** con flash messages y API REST JSON. La revisión anterior cubría hasta `05-flask-04`, por lo que todo lo siguiente es trabajo añadido:
  - `app.secret_key` configurada y `flash(mensaje, categoria)` aplicado en **10 puntos** (3 entidades × varias operaciones CRUD) con categorías diferenciadas (`exito`, `info`).
  - `base.html` renderiza los mensajes con `get_flashed_messages(with_categories=true)` y aplica clases CSS por categoría (`flash-exito`, `flash-info`, `flash-error`).
  - **6 endpoints API REST con `jsonify()`**: colección + detalle para ingredientes, bocadillos y usuarios. El detalle devuelve `{"error": ...}` con HTTP 404 cuando la entidad no existe (verificado con `GET /ingredientes/api/inexistente`).

### Cumple (re-validado del estado anterior)

- `presentation/app.py` con Flask y los blueprints `bp_ingredientes`, `bp_bocadillos`, `bp_usuarios` registrados en su prefijo correspondiente. Buena separación de responsabilidades.
- `@app.errorhandler(404)` y `@app.errorhandler(500)` renderizan `error.html` con título y mensaje.
- Ruta `/ayuda` que lista todas las rutas con `app.url_map.iter_rules()` (excluyendo `static`).
- `@app.before_request` con `app.logger.info` que registra método y ruta en `subwey.log`.
- **8 rutas con `methods=['GET', 'POST']`** (alta y eliminación de las tres entidades, edición de bocadillo y usuario) con patrón Post/Redirect/Get tras éxito.
- 13 plantillas Jinja2 en `templates/` con `base.html` (bloques `{% block title %}` y `{% block content %}`, navegación común).
- Confirmación de eliminación en dos pasos (`GET` muestra confirmación, `POST` ejecuta).

### Verificación funcional

Se probó el flujo end-to-end siguiendo el README:

- **Lecturas**: `GET /`, `/ingredientes/`, `/bocadillos/`, `/usuarios/`, `/ayuda` responden 200.
- **API REST**: `GET /ingredientes/api`, `/bocadillos/api`, `/usuarios/api` y los detalles devuelven JSON correcto; los inexistentes responden 404 con `{"error": ...}`.
- **Alta vía POST**: `POST /ingredientes/nuevo` crea, sale flash de éxito, redirige al listado y persiste tras reiniciar la app.
- **Alta duplicada**: `POST /ingredientes/nuevo` con un nombre ya existente devuelve HTTP 200 re-renderizando el formulario con el mensaje "ya existe" (manejo correcto de la excepción de dominio).
- **Eliminación con FK en uso**: `POST /ingredientes/tomate/eliminar` (tomate está referenciado por bocadillos) devuelve HTTP 409 y mensaje de error claro; `IngredienteEnUsoError` capturada correctamente.
- **Eliminación libre**: `POST /ingredientes/lechuga/eliminar` redirige y el ingrediente desaparece (confirmado por la API).

Todos los flujos funcionan correctamente. Se mantiene la nota 10/10.
