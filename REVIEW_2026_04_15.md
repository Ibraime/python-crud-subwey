# Revisión del proyecto — Ibraime (Subway CRUD)

**Fuente de verdad:** `proyecto/04-sqlite/Subwey/`
**Fases detectadas:** 01 (diseño por capas), 02 (documentación), 03 (testing), 04 (SQLite) en proceso
**Fase revisada en esta sesión:** 01 (Diseño por capas)


## REVISIÓN FASE 01 - 2026-04-15 — Nota: 10/10

### Resuelto desde la revisión anterior (2026-02-20)

- **Presentación acoplada a dominio**: Se ha eliminado completamente
- **Estructura de capas**: Completamente implementada y mantenida en todas las carpetas.
- **POO avanzada**: Se ha añadido `BocadilloPromocion` con herencia, validaciones exhaustivas en setters con propiedades (@property).
- **Entidades suficientes**: Ahora hay tres entidades de dominio: `Ingrediente`, `Bocadillo`, `Usuario` (antes solo `Ingrediente`).

### Cumple

- El proyecto está organizado en capas correctas: `domain/`, `application/`, `infrastructure/`, `presentation/`.
- Los apartados del menú principal funcionan correctamente: registrar ingredientes, consumir, reponer, eliminar, listar; menú bocadillos con creación, modificación, eliminación y listado.
- La estructura de archivos sigue las pautas de módulos, paquetes y subpaquetes Python: cada capa es un paquete con `__init__.py`.
- Se han aplicado los principios de POO: 
  - Clases con responsabilidades claras (`Bocadillo`, `Ingrediente`, `Usuario`).
  - Atributos privados con propiedades (@property/@setter) para encapsulamiento.
  - Herencia en `BocadilloPromocion` que extiende `Bocadillo` con lógica de descuento.
  - Validaciones en setters: nombres no vacíos, precios positivos, stock no negativo, ingredientes no repetidos.
- Los nombres de ficheros, clases y variables son significativos y siguen PEP8:
  - Clases: `Bocadillo`, `Ingrediente`, `Usuario`, `RepositorioBocadillo`, `ServicioBocadillo` (PascalCase).
  - Métodos y variables: `obtener_por_nombre()`, `modificar_bocadillo()`, `es_promocional()` (snake_case).
- README.md presente con instrucciones de instalación y ejecución.

### Aspectos positivos

1. **Arquitectura en capas bien implementada** — Separación clara de responsabilidades entre capas.
2. **Entidades del dominio sólidas** — Validaciones exhaustivas en propiedades, encapsulamiento correcto.
3. **POO avanzada** — Uso de herencia, propiedades, excepciones custom. `BocadilloPromocion` es un buen ejemplo.
4. **Menú funcional** — Flujos de usuario correctos: crear, modificar, eliminar, listar sin errores.
5. **Nombres coherentes** — PEP8 seguido en general; buena nomenclatura de clases y métodos.

## REVISIÓN FASE 02 - 2026-04-15 — Nota: 9/10

### Resuelto desde la revisión anterior (2026-02-20)

- **Documentación completa de `docs/`**: Se han añadido todos los documentos requeridos: arquitectura, casos de uso, reglas de negocio, modelo de dominio, contrato repositorio, datos iniciales, tests/pasos y troubleshooting.
- **README.md actualizado**: Ahora incluye secciones Quickstart, Requisitos, Uso, Estructura y Documentación.
- **CHANGELOG.md presente**: Documenta cambios por versión en las fases.
- **Docstrings en dominio**: Clases y métodos del dominio tienen docstrings completos con parámetros y excepciones.

### Cumple

- Carpeta `docs/` con **todos los documentos requeridos**: DESCRIPCION_Y_ALCANCE.md, EJECUCION.md, ARQUITECTURA_POR_CAPAS.md, CASOS_DE_USO.md, REGLAS_DE_NEGOCIO.md, MODELO_DE_DOMINIO.md, CONTRATO_REPOSITORIO.md, DATOS_INICIALES.md, TESTS_Y_PASOS.md, TROUBLESHOOTING.md.
- README.md con estructura clara: Quickstart, Requisitos, Uso, Estructura, Documentación, Tests.
- CHANGELOG.md con versionado y cambios acumulados por fase.
- Docstrings en módulos (`domain/bocadillo.py`, `domain/ingrediente.py`) formales y completos.
- Docstrings en clases del dominio: `Bocadillo`, `BocadilloPromocion`, `Ingrediente`, `Usuario`.
- Docstrings en setters (@property.setter) documentando excepciones.
- Reglas de negocio comentadas en los setters: validaciones de stock, ingredientes no repetidos, precio positivo, etc.
- Nombres conformes a PEP8 en todo el código.
- Documentación técnica coherente con la implementación: arquitectura, casos de uso, y modelos describen correctamente lo que existe en el código.

### Errores y aspectos a mejorar

#### **[SUGERENCIA] Docstrings faltantes en `application/` (servicios)**

Los servicios (`ServicioBocadillo`, `ServicioIngrediente`, `ServicioUsuario`) tienen comentarios de línea (`# Añade un bocadillo...`) pero carecen de **docstrings de módulo** y **docstrings de clase y métodos públicos**.

**Ejemplo `application/servicios_bocadillo.py`:**
```python
# application/servicios_bocadillo.py

class ServicioBocadillo:
    # Añade un bocadillo a la lista (con o sin descuento)
    def crear_bocadillo(self, nombre, ingredientes, descuento=None, autor=None):
        return self._repo.guardar(...)
```

**Debería ser:**
```python
"""
Módulo con los servicios (casos de uso) para la gestión de bocadillos.
"""

class ServicioBocadillo:
    """
    Servicio que orquesta operaciones sobre bocadillos.
    Coordina la interacción entre presentación, dominio e infraestructura.
    """

    def crear_bocadillo(self, nombre, ingredientes, descuento=None, autor=None):
        """
        Crea un nuevo bocadillo (con o sin descuento).

        Nota: lo siguiente es opcional, pero sigue recomendaciones a la hora de documentar:
        :param nombre: Nombre del bocadillo.
        :param ingredientes: Lista de Ingrediente.
        :param descuento: Porcentaje de descuento (opcional).
        :param autor: Usuario autor (opcional, por defecto "Anónimo").
        :return: Bocadillo creado.
        :raises ValueError: Si los datos son inválidos.
        """
        ...
```

**Docstrings faltantes:**

**`application/servicios_bocadillo.py`** (9 docstrings faltantes)
1. Docstring de módulo (línea 1, antes de imports)
2. Docstring de clase `ServicioBocadillo` (línea 3)
3. Docstring de método `__init__` (línea 5)
4. Docstring de método `crear_bocadillo()` (línea 9)
5. Docstring de método `modificar_bocadillo()` (línea 13)
6. Docstring de método `eliminar_bocadillo()` (línea 17)
7. Docstring de método `listar_bocadillos()` (línea 21)
8. Docstring de método `obtener_bocadillo()` (línea 25)
9. Docstring de método `es_promocional()` (línea 28)

**`application/servicios_ingrediente.py`** (9 docstrings faltantes)
1. Docstring de módulo (línea 1, reemplazar comentarios líneas 2-3)
2. Docstring de clase `ServicioIngrediente` (línea 7)
3. Docstring de método `__init__` (línea 8)
4. Docstring de método `registrar_ingrediente()` (línea 12)
5. Docstring de método `reponer_ingrediente()` (línea 16)
6. Docstring de método `consumir_ingrediente()` (línea 20)
7. Docstring de método `eliminar_ingrediente()` (línea 24)
8. Docstring de método `listar_ingredientes()` (línea 28)
9. Docstring de método `buscar_por_nombre()` (línea 32)

**`domain/usuario.py`** (4 docstrings faltantes)
1. Docstring de módulo (línea 1, reemplazar comentario)
2. Docstring de clase `Usuario` (línea 3)
3. Docstring de método `__init__` (línea 4)
4. Docstring de método `__repr__` (línea 8)

**Total: 22 docstrings faltantes**
- [ ] método `modificar_bocadillo()`
- [ ] método `eliminar_bocadillo()`
- [ ] método `listar_bocadillos()`
- [ ] método `obtener_bocadillo()`
- [ ] método `es_promocional()`

#### **[SUGERENCIA] Comentarios redundantes en `application/servicios_*.py` y `presentation/menu_*.py`**

Hay comentarios de línea que repiten lo obvio del código. **Los comentarios deben explicar el *por qué*, no el *qué*.**

**Comentarios redundantes a eliminar:**

**`application/servicios_bocadillo.py`** — 5 comentarios
- Línea 8: `# Añade un bocadillo a la lista (con o sin descuento)` 
- Línea 12: `# Modifica los ingredientes del bocadillo`
- Línea 16: `# Elimina un bocadillo de la lista`
- Línea 20: `# Muestra todos los bocadillos que existen actualmente en la lista`
- Línea 24: `# Obtiene el bocadillo correspondiente al nombre`

**`application/servicios_ingrediente.py`** — 7 comentarios
- Línea 2-3: Comentarios redundantes de cabecera (convertir a docstring)
- Línea 11: `# Añade un ingrediente a la lista`
- Línea 15: `# Aumenta el stock de ingrediente en una cantidad específica`
- Línea 19: `# Decrementa el stock de ingrediente en una cantidad específica`
- Línea 23: `# Elimina un ingrediente de la lista`
- Línea 27: `# Muestra todos los ingredientes que existen actualmente en la lista`
- Línea 31: `# Obtiene el ingrediente correspondiente al nombre`

**`presentation/menu_ingredientes.py`** — 3 comentarios
- Línea 42: `# Inicializa repositorio y servicio de ingredientes`
- Línea 46: `# Inicializa repositorio y servicio de bocadillos`
- Línea 51: `# Inicializa repositorio y servicio de usuarios`
- (CONSERVAR línea 47: "para mantener consistencia de datos al cambiar de menú" — esto es valioso)

**Cómo resolverlo:** Elimina estos comentarios obvios. Cuando añadas docstrings, los comentarios se vuelven innecesarios.

## REVISIÓN FASE 03 - 2026-04-15 — Nota: 10/10

### Resuelto desde la revisión anterior (2026-02-20)

- **Tests reorganizados en `tests/`**: Todos los tests unitarios están dentro de la carpeta `tests/` con estructura clara.
- **Cobertura de dos clases del dominio**: Tests para `Bocadillo` e `Ingrediente` con casos normales, límites y excepciones.
- **Documentación de tests**: `docs/TESTS_Y_PASOS.md` con instrucciones completas de cobertura.
- **CHANGELOG.md actualizado**: Registra cambios de fase 03 con versión `0.3.1` y `0.3.0`.
- **Requirements.txt actualizado**: Incluye `coverage==7.13.4` como dependencia.

### Cumple

- Carpeta `tests/` organizada con 4 ficheros de tests unitarios:
  - `test_bocadillo.py`: 14 tests de `Bocadillo` y `BocadilloPromocion` —  14 pasan
  - `test_ingrediente.py`: 10 tests de `Ingrediente` —  10 pasan
  - `test_repositorio_bocadillo.py`: 11 tests de persistencia en memoria —  11 pasan
  - `test_repositorio_ingrediente.py`: 16 tests de persistencia en memoria —  16 pasan
- **Total: 51 tests ejecutados, 51 pasan (100%)**
- Tests validan entidades y repositorios: creación, validaciones, excepciones, límites.
- `requirements.txt` incluye `coverage==7.13.4`.
- `docs/TESTS_Y_PASOS.md` documenta:
  - Preparación del entorno virtual
  - Ejecución de tests completos: `python -m unittest`
  - Ejecución por módulo (ejemplos incluidos)
  - Coverage: `coverage run -m unittest` y `coverage report`
  - Generación de reportes HTML
- `CHANGELOG.md` con versiones `0.3.1` y `0.3.0` registrando cambios de fase.
- Documentación actualizada en `docs/EJECUCION.md` con pasos de preparación del entorno.
- README.md menciona tests y estructura de fases.

### Errores y aspectos a mejorar

**Ninguno detectado.** Cumple completamente con los requisitos:


### Checklist Fase 03 — Estado

| Criterio | Cumple | Notas |
|---|---|---|
| Carpeta `tests/` presente | ✓ | 4 ficheros organizados |
| Tests de 2+ clases dominio | ✓ | Bocadillo, Ingrediente cubiertos |
| Tests pasan con unittest | ✓ | 51/51 tests pasan (100%) |
| Coverage en requirements.txt | ✓ | `coverage==7.13.4` presente |
| Coverage ejecutable | ✓ | Comando `coverage run -m unittest` funciona |
| TESTS_Y_PASOS.md actualizado | ✓ | Con instrucciones completas |
| EJECUCION.md actualizado | ✓ | Con pasos de venv y entorno |
| CHANGELOG.md con versión 0.3.0 | ✓ | Versiones `0.3.0` y `0.3.1` presentes |
| README.md actualizado | ✓ | Menciona tests y estructura |


### Aspectos positivos de Fase 03

1. **Cobertura exhaustiva de entidades** — Tests validan crear, validar, excepciones y límites en `Bocadillo` e `Ingrediente`.
2. **Repositorios en memoria bien probados** — 27 tests verifican CRUD, búsqueda, eliminación y listado.
3. **100% de tests pasando** — Ejecución limpia sin errores.
4. **Documentación clara** — Instrucciones de ejecución y cobertura bien detalladas.
5. **Estructura modular** — Cada fichero de tests es independiente y ejecutable.



